<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{promo.title}}</title>
  <style>
    @page { size: letter; margin: 0; }

    html, body {
      width: 8.5in;
      height: 11in;
      margin: 0;
      padding: 0;
      font-family: Arial, Helvetica, sans-serif;
      box-sizing: border-box;
    }

    *, *::before, *::after { box-sizing: border-box; }

    .page {
      width: 8.5in;
      height: 11in;
      display: flex;
      flex-direction: column;
      overflow: hidden;
      background: #fff;
    }

    /* Top accent bar */
    .accent-bar {
      height: 8px;
      background: {{brand.secondaryColor}};
      flex-shrink: 0;
    }

    /* Header */
    .header {
      background: #fff;
      padding: 20px 32px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      border-bottom: 2px solid {{brand.primaryColor}};
      flex-shrink: 0;
    }

    .header-logo { height: 48px; width: auto; object-fit: contain; }
    .header-company {
      font-size: 24px;
      font-weight: 800;
      color: {{brand.primaryColor}};
    }

    .header-label {
      font-size: 13px;
      font-weight: 600;
      color: {{brand.secondaryColor}};
      text-transform: uppercase;
      letter-spacing: 1.5px;
    }

    /* Hero title */
    .hero {
      background: {{brand.primaryColor}};
      padding: 22px 32px;
      flex-shrink: 0;
    }

    .hero-label {
      font-size: 12px;
      font-weight: 600;
      color: rgba(255,255,255,0.7);
      text-transform: uppercase;
      letter-spacing: 2px;
      margin: 0 0 6px 0;
    }

    .hero-title {
      font-size: 34px;
      font-weight: 900;
      color: #fff;
      margin: 0 0 4px 0;
      line-height: 1.15;
    }

    .hero-sub {
      font-size: 16px;
      color: rgba(255,255,255,0.85);
      margin: 0;
    }

    /* Products */
    .products-section {
      flex: 1;
      padding: 20px 32px;
      background: #fafafa;
      overflow: hidden;
    }

    .product-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 14px;
    }

    .product-card {
      background: #fff;
      border: 1px solid #e0e0e0;
      border-left: 4px solid {{brand.secondaryColor}};
      border-radius: 6px;
      padding: 14px 16px;
    }

    .product-vendor {
      font-size: 10px;
      font-weight: 600;
      color: {{brand.secondaryColor}};
      text-transform: uppercase;
      letter-spacing: 1px;
      margin: 0 0 4px 0;
    }

    .product-name {
      font-size: 14px;
      font-weight: 700;
      color: #1a1a1a;
      margin: 0 0 6px 0;
      line-height: 1.3;
    }

    .product-price {
      font-size: 22px;
      font-weight: 900;
      color: {{brand.primaryColor}};
      margin: 0;
    }

    .product-unit {
      font-size: 11px;
      color: #999;
      margin: 2px 0 0 0;
    }

    /* Footer */
    .footer {
      background: {{brand.secondaryColor}};
      padding: 14px 32px;
      text-align: center;
      flex-shrink: 0;
    }

    .footer-cta {
      color: #fff;
      font-size: 17px;
      font-weight: 700;
      margin: 0;
      letter-spacing: 0.5px;
    }

    /* Watermark */
    .watermark {
      position: fixed;
      top: 50%; left: 50%;
      transform: translate(-50%, -50%) rotate(-45deg);
      font-size: 80px;
      color: rgba(0,0,0,0.07);
      font-weight: 900;
      pointer-events: none;
      white-space: nowrap;
      z-index: 999;
    }
  </style>
</head>
<body>
  <div class="page">

    <div class="accent-bar"></div>

    <!-- Header -->
    <div class="header">
      <div style="display:flex;align-items:center;gap:12px;">
        {{#if brand.logoUrl}}
          <img class="header-logo" src="{{brand.logoUrl}}" alt="{{brand.name}}">
        {{else}}
          <span class="header-company">{{brand.name}}</span>
        {{/if}}
      </div>
      <span class="header-label">Vendor Spotlight</span>
    </div>

    <!-- Hero -->
    <div class="hero">
      <p class="hero-label">Featured Products</p>
      <p class="hero-title">{{promo.title}}</p>
      {{#if promo.subhead}}
        <p class="hero-sub">{{promo.subhead}}</p>
      {{/if}}
    </div>

    <!-- Products -->
    <div class="products-section">
      <div class="product-grid">
        {{#each items}}
          <div class="product-card">
            {{#if vendor}}
              <p class="product-vendor">{{vendor}}</p>
            {{/if}}
            <p class="product-name">{{name}}</p>
            <p class="product-price">{{price}}</p>
            {{#if unit}}
              <p class="product-unit">per {{unit}}</p>
            {{/if}}
          </div>
        {{/each}}
      </div>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p class="footer-cta">
        {{#if promo.cta}}{{promo.cta}}{{else}}Ask about our full product range{{/if}}
      </p>
    </div>

  </div>

  {{#if watermark}}
    <div class="watermark">PREVIEW ONLY</div>
  {{/if}}
</body>
</html>
